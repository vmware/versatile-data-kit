apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vdk-ingress
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: "vdk-svc"
                port:
                  number: 8092